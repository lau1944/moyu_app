
import 'package:flutter/material.dart';

class MacMoyuView extends StatelessWidget {
  const MacMoyuView({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}